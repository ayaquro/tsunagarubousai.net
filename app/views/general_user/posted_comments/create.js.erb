$("#comments_area").html("<%= j(render 'general_user/posted_comments/posted_comments', post: @post, posted_comment: @posted_comment, posted_comments: @posted_comments) %>");
<!--フォームを送信したあとにフォームの値を空にします-->
$("textarea").val('');