<!DOCTYPE html>
<html>
  <head>
    <title>TsunagarubousaiNet</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <!--管理者がログイン状態の時-->
      <% if staff_signed_in? %>
      <il>
        <%= link_to "投稿一覧", staff_posts_path %>
      </il>
      <il>
        <%= link_to "会員一覧", staff_general_users_path %>
      </il>
      <il>
        <%= link_to "地区一覧", staff_districts_path %>
      </il>
      <il>
        <%= link_to "ログアウト", destroy_staff_session_path, method: :delete %>
      </il>
      <!--一般ユーザーがログイン状態の時-->
      <% elsif general_user_signed_in? %>
      <il>
        <%= link_to "危険箇所の投稿", new_post_path %>
      </il>
      <il>
        <%= link_to "マイページ", general_user_path(current_general_user.id) %>
      </il>
      <il>
        <%= link_to "ログアウト", destroy_general_user_session_path, method: :delete %>
      </il>
      <!--ログアウト状態の時-->
      <% else %>
      <il>
        <%= link_to "新規会員登録", new_general_user_registration_path %>
      </il>
      <il>
        <%= link_to "ログイン", new_general_user_session_path %>
      </il>
      <% end %>
    </header>
    <%= yield %>
  </body>
</html>
